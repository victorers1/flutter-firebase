import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:firebase_flutter/app/modules/performance/performance_page.dart';

void main() {
  testWidgets('PerformancePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PerformancePage(title: 'Performance')));
    //  final titleFinder = find.text('Performance');
    //  expect(titleFinder, findsOneWidget);
  });
}
