import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:firebase_flutter/app/modules/performance/performance_module.dart';

void main() {
  initModule(PerformanceModule());
  // PerformanceController performance;
  //
  setUp(() {
    //     performance = PerformanceModule.to.get<PerformanceController>();
  });

  group('PerformanceController Test', () {
    //   test("First Test", () {
    //     expect(performance, isInstanceOf<PerformanceController>());
    //   });

    //   test("Set Value", () {
    //     expect(performance.value, equals(0));
    //     performance.increment();
    //     expect(performance.value, equals(1));
    //   });
  });
}
