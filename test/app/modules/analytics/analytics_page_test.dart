import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AnalyticsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AnalyticsPage(title: 'Analytics')));
    //  final titleFinder = find.text('Analytics');
    //  expect(titleFinder, findsOneWidget);
  });
}
