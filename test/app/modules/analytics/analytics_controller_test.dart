import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:firebase_flutter/app/modules/analytics/analytics_module.dart';

void main() {
  initModule(AnalyticsModule());
  // AnalyticsController analytics;
  //
  setUp(() {
    //     analytics = AnalyticsModule.to.get<AnalyticsController>();
  });

  group('AnalyticsController Test', () {
    //   test("First Test", () {
    //     expect(analytics, isInstanceOf<AnalyticsController>());
    //   });

    //   test("Set Value", () {
    //     expect(analytics.value, equals(0));
    //     analytics.increment();
    //     expect(analytics.value, equals(1));
    //   });
  });
}
